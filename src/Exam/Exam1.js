const Exam1 = () => {
    return (    // 부모는 한 명만 있어야함.
        // xml과 성향이 비슷하다.
        // class => className
        // for => forHTML
        // class명 사용할때 상단처럼 저렇게 사용하는 것.
        // 저것 두개만 다를 뿐 비슷함.
        <div className="ddd">
            <br/>
            <h1>Hello</h1>
        </div>
    );
}

export default Exam1;   // 외부에서도 이 파일(Exam1)을 사용할 수 있도록 열어주는 것과 같다.